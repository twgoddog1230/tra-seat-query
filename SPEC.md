# 臺鐵座位查詢系統 開發規格書

**版本**：1.0  
**最後更新**：2026-05-31  
**GitHub**：https://github.com/twgoddog1230/tra-seat-query  
**正式網址**：https://tra-seat-query-production.up.railway.app

---

## 1. 系統概述

### 目的
提供不特定使用者在任何裝置（手機、平板、電腦）上查詢臺鐵座位資訊，輸入起訖站、車種、車廂號與座位號，即可判斷：
- 該座位是**靠窗**或**靠走道**
- 若靠窗，是否**靠海**（顯示 🌊 圖示及海洋名稱）
- 自動判斷行駛**路線名稱**與**方向**

### 使用範圍
- 東部幹線（北迴線、花東線）
- 南迴線
- 西部幹線（山線、海線）
- 跨線旅程（東部↔西部、東部↔南迴）

---

## 2. 技術規格

| 項目 | 規格 |
|------|------|
| 框架 | Next.js 16.2.6（App Router） |
| 語言 | TypeScript |
| UI 框架 | Tailwind CSS v4 |
| React | 19.2.4 |
| 部署平台 | Railway（自動 CI/CD） |
| 原始碼 | GitHub（twgoddog1230/tra-seat-query） |
| 資料儲存 | 純靜態 TypeScript（無後端、無資料庫） |

---

## 3. 專案目錄結構

```
src/
├── app/
│   ├── layout.tsx          # HTML meta、viewport、全局 layout
│   ├── page.tsx            # 主頁面（所有互動邏輯）
│   └── globals.css         # 全局樣式、Tailwind 自訂色彩
├── components/
│   ├── StationInput.tsx    # 車站輸入框（含即時自動補全）
│   ├── TrainTypeSelector.tsx # 車種選擇按鈕群組
│   ├── CarSeatInput.tsx    # 車廂號 + 座位號輸入
│   └── ResultCard.tsx      # 查詢結果卡片（票卡樣式）
├── data/
│   ├── stations.ts         # 全臺車站資料庫（線別、排序）
│   └── trainRules.ts       # 各車種各車廂靠海規則
└── lib/
    └── analyzeSeat.ts      # 核心分析邏輯
```

---

## 4. 支援車種

| 車種 ID | 顯示名稱 | 車廂數 |
|--------|---------|------|
| `EMU3000` | 新自強號（EMU3000） | 12 節 |
| `Puyuma` | 普悠瑪號 | 8 節 |
| `Taroko` | 太魯閣號 | 8 節 |
| `PP` | 自強號（PP 推拉式） | 12 節 |
| `Chuguang` | 莒光號 | 不固定 |

---

## 5. 靠窗／靠走道判斷規則

所有車種通用：

```
座位號 ÷ 4 的餘數 = 1 或 2 → 靠窗
座位號 ÷ 4 的餘數 = 3 或 0 → 靠走道
```

範例：1, 2, 5, 6, 9, 10… → 靠窗；3, 4, 7, 8, 11, 12… → 靠走道

---

## 6. 靠海判斷規則

### 6.1 基本原理

- 奇號（單號）座位 與 偶號（雙號）座位，分別位於車廂兩側
- 「靠海」僅對**靠窗座位**顯示
- 靠海海洋名稱依路線而定：東部幹線、南迴線 → **太平洋**；西部海線 → **台灣海峽**；西部山線 → 無

### 6.2 東部幹線（花東線、北迴線）

靠海座位由座位在車廂中的**物理位置**決定，**與行駛方向無關**。
太平洋永遠在臺灣東側，偶號（或單號）座位永遠在同一側，不因北上或南下而改變。

| 車種 | 車廂範圍 | 靠海座位（恆定，不分方向） |
|------|---------|--------------------------|
| 新自強號（EMU3000） | 1～8 車 | 雙號 |
| 新自強號（EMU3000） | 9～12 車 | 單號 |
| 普悠瑪號 | 1～4 車 | 雙號 |
| 普悠瑪號 | 5～8 車 | 單號 |
| 太魯閣號 | 1～4 車 | 雙號 |
| 太魯閣號 | 5～8 車 | 單號 |
| 自強號（PP） | 1、7、12 車 | 單號 |
| 自強號（PP） | 其他車廂 | 雙號 |
| 莒光號 | 全車廂 | 單號 |

> 此規則經乘客實際乘車驗證（普悠瑪 1 車 5 號北上 = 靠山、6 號北上 = 靠海）確認正確。

### 6.3 南迴線（枋寮↔台東）

與東部幹線規則相同（物理位置固定，不分方向）。

南迴線支援車種：EMU3000、普悠瑪號。

### 6.4 西部海線

規則與東部幹線相同（物理位置固定，不分方向）。

### 6.5 西部山線

無特定靠海側，不顯示靠海資訊。

---

## 7. 路線與方向判斷邏輯

### 7.1 路線自動判斷

1. 查詢起站與訖站所在線別
2. 若兩站在同一線 → 單線旅程
3. 若不同線 → 跨線旅程，分段處理

### 7.2 方向判斷

每個車站有路線序號（seq），序號越大代表越往南／往東：

```
訖站 seq > 起站 seq → 南下（southbound）
訖站 seq < 起站 seq → 北上（northbound）
```

### 7.3 路線名稱顯示

| 條件 | 顯示標籤 |
|------|---------|
| 東部幹線，起訖均在花蓮以北（含） | 北迴線（往花蓮／往台北方向） |
| 東部幹線，起訖均在花蓮以南（含） | 花東線（往台東／往花蓮方向） |
| 東部幹線，跨越花蓮 | 南下（往台東方向）/ 北上（往台北方向） |
| 南迴線，往台東 | 南迴線（往台東方向） |
| 南迴線，往枋寮 | 南迴線（往枋寮方向） |
| 西部山線 | 南下（往高雄方向）/ 北上（往台北方向） |
| 西部海線 | 海線（往彰化方向）/ 海線（往竹南方向） |

### 7.4 跨線旅程分段

| 旅程類型 | 分段顯示 |
|---------|---------|
| 西部山線 → 東部幹線 | 段一：山線到台北（無海景）；段二：台北到目的地（太平洋） |
| 東部幹線 → 南迴線 | 段一：東部到台東（太平洋）；段二：台東到目的地（太平洋） |
| 反向亦然 | 對調顯示 |

---

## 8. 車站資料庫

### 8.1 線別分類

| LineType | 說明 | 車站範圍 |
|----------|------|---------|
| `east` | 東部幹線 | 台北 → 台東 |
| `south-link` | 南迴線 | 台東 → 枋寮 |
| `west-mountain` | 西部山線 | 基隆 → 屏東 |
| `west-coast` | 西部海線 | 竹南 → 彰化（追分） |

### 8.2 共用站處理

部分車站屬於多條線的交會點，在資料庫中重複建立：

| 車站 | 所在線別 |
|------|---------|
| 台北 | east、west-mountain |
| 台東 | east、south-link |

### 8.3 站名正規化

搜尋時自動將「臺」轉換為「台」比對，兩種寫法均可查詢（如「臺北」＝「台北」）。

---

## 9. UI 規格

### 9.1 設計風格

- 主題：臺鐵票卡風格
- 色系：鐵路深藍（`#0e1f2f`）+ 金色（`#c9a84c`）
- 火車元素：Header 含火車剪影 SVG、結果卡片仿票根設計

### 9.2 互動流程

```
① 輸入起站（自動補全）
② 輸入訖站（自動補全）
   ↕ 可點「⇅」交換起訖站
③ 選擇車種（按鈕選擇）
④ 輸入車廂號
⑤ 輸入座位號
⑥ 點「查詢座位」→ 顯示結果卡片
```

### 9.3 結果顯示

- 路線標籤（如「花東線（往台東方向）」）
- 車廂號 + 座位號
- 靠窗 🪟 或 靠走道 🚶
- 若靠窗且靠海：顯示 🌊（太平洋／台灣海峽）
- 若靠窗但靠山：顯示 🏔
- 跨線旅程：顯示分段提示，說明各段是否有海景

### 9.4 響應式設計

- 手機優先（mobile-first）
- 車種選擇：手機 2 欄、桌機 3 欄
- 所有輸入框支援手機鍵盤（`inputMode="numeric"`）

---

## 10. 常用查詢功能

### 10.1 最近查詢記錄（自動）

每次點擊「查詢座位」後，系統自動記錄：
- 起訖站、車種、車廂號、座位號
- 時間戳記（用於排序）
- 最多保留最近 6 筆

點擊任一筆記錄 → 自動填入所有欄位，可再次查詢或修改後查詢。

### 10.2 我的最愛（手動）

使用者可將常用的起訖站組合加入最愛：
- **新增方式**：從「最近查詢」的記錄中點 ⭐ 一鍵加入，或手動選擇起訖站後加入
- **記錄內容**：僅儲存起訖站（不含車廂/座位號）
- **使用方式**：點擊任一最愛 → 自動填入起訖站，再輸入車種與座位查詢
- **上限**：最多 6 筆

### 10.3 資料儲存

使用瀏覽器 **localStorage**（本機端儲存）：
- 下次造訪自動載入歷史和最愛
- 不同裝置間無法同步（如在手機上記的最愛，電腦上看不到）

---

## 11. 部署架構

```
開發者 push → GitHub (main branch)
                    ↓ 自動觸發
              Railway CI/CD
                    ↓
           next build（靜態輸出）
                    ↓
            Railway 公開網址
```

- 無環境變數需求（純靜態前端）
- 無資料庫、無 API 呼叫

---

## 12. 版本歷程

| 版本 | 日期 | 內容 |
|------|------|------|
| v1.0.0 | 2026-05-31 | 初始版本：東部幹線各車種靠窗／靠海查詢 |
| v1.1.0 | 2026-05-31 | 支援「台」「臺」兩種寫法互通查詢 |
| v1.2.0 | 2026-05-31 | 新增南迴線靠海判斷規則 |
| v1.3.0 | 2026-05-31 | 細分路線名稱（北迴線／花東線／南迴線） |
| v1.4.0 | 2026-06-04 | 修正靠海判斷邏輯：方向不影響靠海座位（實車驗證）|
| v1.5.0 | 2026-06-04 | 新增常用查詢功能：最近查詢記錄 + 自訂我的最愛 |

---

## 13. 已知限制與未來方向

| 項目 | 說明 |
|------|------|
| 騰雲座艙 | 目前不支援（EMU3000 第 6 車廂商務艙）|
| 南迴線車種 | 目前僅支援 EMU3000 與普悠瑪，其他車種未驗證 |
| 西部幹線靠海規則 | 已實作邏輯，尚未實際驗證正確性 |
| 跨三線旅程 | 如屏東→花蓮（西部→南迴→東部）目前無法正確分段 |
| 座位圖視覺化 | 尚未實作 |
